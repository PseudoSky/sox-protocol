"""SOX Protocol adapters — runtime and backing-store implementations.

This package MUST NOT be imported by ``sox_protocol.core``.
Enforced by import-linter (see pyproject.toml [tool.importlinter]).
"""
